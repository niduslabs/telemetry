It is assumed throughout this documentation that you have Node.JS and npm already installed on your PC

To get started, firstly install the npm dependencies:
`npm install`

Edit lines 7, 9, 13

To run demonstrate telemetry readings:
`node telemetry.js`

